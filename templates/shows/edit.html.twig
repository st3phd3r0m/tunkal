{% extends 'base-admin.html.twig' %}

{% block title %}Editer concert{% endblock %}

{% block body %}
	<!-- Content Header (Page header) -->
	<div class="content-header">
		<div class="container-fluid">
			<div class="row mb-2">
				<div class="col-sm-6">
					<h1 class="m-0 text-dark">Editer concert</h1>
				</div>
				<!-- /.col -->
				<div class="col-sm-6">
					<ol class="breadcrumb float-sm-right">
						<li class="breadcrumb-item">
							<a href="{{ path('admin') }}">Dashboard</a>
						</li>
						<li class="breadcrumb-item active">Concerts</li>
					</ol>
				</div>
				<!-- /.col -->
			</div>
			<!-- /.row -->
		</div>
		<!-- /.container-fluid -->
	</div>
	<!-- /.content-header -->

	<!-- Main content -->
	<div class="content">
		<div class="container-fluid">

            {{ include('shows/_form.html.twig', {'button_label': 'Modifier'}) }}

            <a href="{{ path('shows_index') }}">back to list</a>

            {{ include('shows/_delete_form.html.twig') }}

    	</div>
	</div>
{% endblock %}
{% block javascripts %}
	{{ encore_entry_script_tags('adminShows') }}
{% endblock %}
